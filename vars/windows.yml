---

installation_folder: "C:\\Experitest\\cloud-agent"

service_name: "{{ app_name }}"

installer_file_name: "dist-Windows-AGENT-{{ app_version }}.zip"

download_url : "https://{{ cdn_name }}/{{download_relative_path}}/{{ installer_file_name }}"
