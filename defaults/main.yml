---

app_name: cloudagent

service_name: "com.experitest.{{ app_name }}"

clear_temp_folder: False

s3_bucket_name: seetest-io-build-artifacts

main_class: "com.experitest.cloudagent.MainForwarder"

server_port: 8081
jmx_port: 51234

start_after_install: True

clear_installation_folder_before_install: False

download_from_s3: True

installer_file_name: "{{ app_name }}_{{ app_version }}.zip"
